# 更新日志

本文件记录 vibe-invest-us 各公开版本的重要变化。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- 版本化盈利保护计划，支持 R 阶梯、最大仓位、财报风险窗口和持仓变化复核；
- Tracking 驱动的 EMA20 移动保护、计划期内价格高水位、利润回吐与幂等待处理提醒；
- 可通过 `TRACKING_SCAN_INTERVAL_MINUTES` 启用实例内部定时扫描，提醒确认不触发自动交易。
- 可选用 `PORTFOLIO_MCP_TOKEN` 开放私有持仓 MCP：读取持仓与流水、按幂等操作记录已成交买卖、校准持仓和调整现金；未配置令牌时不注册路由。

### 变更

- 制定盈利保护计划时基准价改为自动读取制定时的现价，用户只需填写失效价；无可用行情时回退为平均成本并在表单提示；修订计划沿用该版已冻结的基准价，仍可手动改写。
- 行情取数改为按来源优先级依次尝试、首个可用来源即被采用，单次尝试上限 2 秒，不再等待最慢的备用来源；
- 组合页新增「刷新行情」按钮和行情观测时间/来源展示；组合与盈利保护在短 TTL 缓存窗口内共用一次行情取价（`MARKET_PRICE_CACHE_TTL_MS`，默认 10 秒，`0` 关闭）。

## [0.1.0] - 2026-08-12

首个可用的开源自托管版本。

### 新增

- 单标的按需 AI 分析闭环，综合行情、历史走势、新闻、财报、估值和个人持仓；
- 手工维护持仓和 USD 现金，展示组合总值、仓位分布、盈亏贡献与资产构成；
- 按成交价记录减仓，自动更新现金、剩余持仓和已实现盈亏；
- 组合权益历史和盘中/收盘快照展示；
- 行情主备来源、双新闻源、SEC 财报以及首批科技行业估值；
- 由 Python 确定性计算技术指标和估值，AI 只负责解释与情景分析；
- 基于 Pi 的服务端模型适配，支持用户配置兼容端点、模型和 API Key；
- 异步分析任务、SSE 进度、取消、并发限制和服务重启中断处理；
- 分析快照、原子事实、工具调用轨迹和报告依据的持久化与回放；
- 研究记录的自动保存、标记、备注、重新打开和删除；
- Docker Compose 自托管，以及 PostgreSQL 数据持久化。

### 已知限制

- 组合权益历史从启用该能力后开始积累，不自动补造历史数据；
- 当前由组合页面请求生成盘中快照，尚未提供每日收盘定时任务；
- 持仓扫描和主动提醒尚未实现。
